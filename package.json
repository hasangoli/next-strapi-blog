{
  "name": "next-strapi-blog",
  "version": "1.0.0",
  "description": "A blog app with next js and strapi",
  "scripts": {
    "git": "git add . && git commit -m initial && git pull && git push",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run develop --prefix backend",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "author": "Hasan Goli",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
